<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/search"
        android:title="@string/search"
        android:icon="@drawable/ic_search_white_24dp"
        app:showAsAction="collapseActionView|ifRoom">
        <menu>
            <item android:id="@+id/search_movie"
                android:title="@string/search_movie" />
            <item android:id="@+id/search_tvshow"
                android:title="@string/Search_tvshow" />
        </menu>
    </item>

    <item android:id="@+id/action_change_settings"
        android:title="@string/change_language_settings" />
    <item android:id="@+id/reminder"
        android:title="@string/reminder" />
</menu>